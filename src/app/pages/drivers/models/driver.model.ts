export interface Driver{
    id: string;
    name: string;
    surname: string;
    email: string;
    phone: string;
}

export interface Owner{
    id: string;
    name: string;
    surname: string;
    email: string;
    phone: string;
}